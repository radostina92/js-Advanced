function solve(first, second, third){
  let total = 0;
  total += first.length;
  total += second.length;
  total += third.length;
  let average = Math.floor(total / 3);

  console.log(total);
  console.log(average);
}
